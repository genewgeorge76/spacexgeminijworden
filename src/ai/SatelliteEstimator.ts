export class SatelliteEstimator {
  /**
   * Placeholder estimator until orbital imagery integration is wired in.
   */
  static async calculateSquareFootage(_targetAddress: string): Promise<number> {
    return 10000;
  }
}
