import { createFileRoute } from '@tanstack/react-router';
import PreConOmniNode from '../components/PreConOmniNode';

export const Route = createFileRoute('/pre-con')({
  component: PreConOmniNode,
});
